import 'package:flutter/material.dart';

class NearbyStoreView extends StatelessWidget {
  const NearbyStoreView({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('NearbyStoreView');
  }
}
